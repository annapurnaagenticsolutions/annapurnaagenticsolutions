# Production Checklist — v1.5

1. Run all v1.5 structural/contracts tests.
2. Use GitHub Actions Pages deployment if automatic shared temporal refresh is desired.
3. Confirm `temporal-state.json` refreshes only when `generationIntervalDays` has elapsed.
4. Do not enable weather until a deliberate authoritative brand home-base is chosen.
5. Run Lighthouse for Home, Explore, Living Lab and Evidence.
6. Compare v1.5 LCP/INP against v1.4; reject or cut sensory effects if either regresses >10%.
7. Test `prefers-reduced-motion`, save-data/low-power fallback and keyboard/mobile interaction.
8. Verify `Why today?` explains the shared temporal source and truth boundary.
9. Verify Page Guide remains dismissible and the site functions without JS.
10. Retarget canonical metadata before custom-domain production cutover.
