# Journey Frame Contract v3.4

1. `#living-response` remains a direct child of `.v3-story-copy[data-structural-anchor="home-story"]`.
2. Adaptive intent must not reparent any element contained by a structural anchor.
3. Physical adaptive reordering is restricted to adaptive blocks that are direct children of `main`.
4. Journey copy inherits the same desktop wrap alignment as Establish and Focus.
5. The persistent `.v3-world-rail` remains visible while `body[data-v3-beat="journey"]` is active.
6. `.v3-route-overlay` remains inside the persistent `.v3-stage`; Journey must not create a replacement theatre.
7. Enterprise/Learning/MSME/Design intent changes continue to alter route, primary world, CTAs and depth.
8. Reduced-motion and mobile fallbacks remain unchanged.
