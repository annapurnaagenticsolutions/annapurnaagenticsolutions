# Session 004 — Playability Upgrade

This release deliberately deepens five existing games rather than adding more activities. The primary interaction model shifts from read/type/respond toward arena tactics, drag/tap construction, visual discovery, spatial movement and real-time survival.

Verification records are captured separately in `VERIFICATION.txt`. Real browser screenshot QA remains a separate release gate if the execution environment cannot initialize Chromium graphics reliably.
